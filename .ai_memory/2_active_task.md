# 当前任务

## 任务概述

- 任务：继续整理浏览器管理相关表格布局，解决代理池与实例列表在小窗/定宽表格下的信息挤压与重叠问题
- 当前阶段：已完成实例列表重叠修复并重启应用，当前进入按页面逐个收口表格布局的阶段

## 当前状态

- 代理池页面已切换为更稳定的表格定宽布局，减少各列内容互相挤压导致的松散感
- 筛选区已重排为左侧检索筛选、右侧自动刷新与全选、独立的批量删除入口，整体更容易扫读
- 主表已移除 `来源 / 服务器 / 端口 / 类型` 四列，其中 `类型` 已压入节点名称后的徽标，其他低频信息转移到详情弹层
- 代理名称列已去掉普通节点的重复地理信息，仅保留节点主标题、协议标记与必要标签
- IP 健康列已压缩为更清晰的三层信息结构，并保留详情入口
- 操作列已固定为单行图标按钮区，删除按钮缩为更轻量的危险态，不再出现文字挤压或纵向堆叠感
- 主表整行已支持点击打开 `代理详情`，用于查看来源、服务器、端口、刷新方式与原始代理配置
- 实例列表页面已补充关键列固定宽度，并将操作区改为圆形图标按钮，避免 `上次更新` 与 `操作` 列在小宽度下重叠
- 实例列表第二轮已进一步压缩 `实例名称 / 核心 / 代理 / 快捷打开码 / 关键字 / 上次更新` 的展示宽度，同时将操作按钮放大到更易点击的尺寸
- 实例列表第三轮已将 `核心 / 快捷打开码 / 上次更新` 从主表隐藏到实例详情弹层，并在 `代理` 列复用代理池的 IP 健康国旗 SVG
- 后端 `sing-box` 解析器已补充 AnyTLS，兼容 URI 与 Clash/Mihomo YAML
- 已新增 AnyTLS 解析单测并通过
- 已于 2026-03-31 15:34 重新执行前端构建并重启 Wails 开发进程
- 当前已观察到新的 `wails.exe` 与 `ant-chrome-dev.exe` 运行中，启动日志显示代理数据正常加载

## 阻塞与风险

- 尚未根据真实 AnyTLS 节点做一次端到端手工验证
- 国旗显示优先依赖 `IPPure` 返回的国家码字段；若接口只返回非常规国家名，则会退回到前端内置映射表
- 本机 Wails CLI 为 `v2.12.0`，已将项目 Wails 依赖从 `v2.11.0` 对齐到 `v2.12.0`；后续如需保持与上游完全一致，需要再决定是否回退

## 下一步

- 继续检查其他使用通用 `Table` 的页面，确认在 `table-fixed` 前提下没有遗漏的列宽重叠问题
- 继续根据实例列表的小窗观感决定是否进一步将低频信息并入详情页或卡片视图
- 根据用户在小窗场景下的实际观感，继续决定是否压缩 `IP 健康` 模块或改为更紧凑的两层结构
- 如用户开始导入真实 AnyTLS 节点，优先做一次端到端连通性验证
