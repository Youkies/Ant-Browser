# 当前任务

## 任务概览

- 当前阶段：已完成“去除容量限制 / 去除扩容系统”的主实现。
- 最近完成：删除控制台与实例列表中的扩容相关 UI，移除实例创建/复制数量上限，删除兑换码与额度状态同步逻辑，并清理隐藏管理员发卡页。
- 当前剩余事项：若用户确认界面无误，可进入下一项需求或决定是否提交本次改动。

## 最近已完成事项

- 控制台的“扩容系统”区块、兑换码输入、GitHub Star 赠送入口已删除。
- 实例列表中的“扩容实例”按钮与扩容弹窗已删除。
- 实例创建与复制不再受容量上限约束。
- 后端已删除兑换码、额度状态同步和相关测试逻辑。
- `AppConfig` 不再维护 `max_profile_limit` / `used_cd_keys` 作为运行期能力。
- 隐藏管理员页面 `/admin/keygen` 已移除，个人页连续点击不再跳转到发卡页。
- Wails bindings 已重新生成，前端构建与 `go build ./...` 已通过。

## 待验证与风险

- 当前未重新启动开发环境，需要用户打开运行中的应用或下次启动时确认控制台和实例列表里的扩容元素确已消失。
- `go test ./backend/...` 仍有两条 `backend/internal/apppath` 的 Darwin 路径测试在 Windows 环境失败，看起来与本次去扩容改动无关，但暂未处理。
- `config.yaml` 仍是本地运行态变更，提交时应继续单独判断是否纳入版本控制。

## 下一步建议

- 请用户先确认控制台与实例列表页面上已无扩容相关入口，且新增/复制实例不再报容量上限错误。
- 若用户准备提交，本次可以单独整理一笔“移除扩容系统”的提交。
