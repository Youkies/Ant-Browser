# 归档上下文

## 2026-03-31 初始化记忆库
- 初始化仓库本地记忆库。
- 初始阶段仅确认了目录状态，随后接入用户 fork 的 `Youkies/Ant-Browser` 仓库。
- 后续记忆以“已读代码 + 已验证结果 + 用户明确确认”为准持续更新。

## 2026-03-31 桌面端产品化收口
- 先围绕代理池与实例列表做小窗适配，逐步把低频信息移入详情，把高频动作改成图标按钮。
- 在代理链路上补齐了 `AnyTLS` 与单层前置节点，目标是让代理池配置一次即可被实例自动复用。
- 在桌面体验上把 Wails 路由切到 `HashRouter`，再继续推进主题统一、自定义标题栏和图标替换。
- 在业务收口上去掉了容量限制、扩容系统、兑换码与隐藏发卡页，保留更直接的本地管理模型。

## 2026-03-31 浏览器启动行为调整
- 用户明确要求浏览器实例只在第一次启动时打开 `https://ippure.com/`、`https://iplark.com/`、`https://ping0.cc/`。
- 实现上新增实例级持久标记 `initialVerificationDone`，首次成功启动后落库，后续启动不再注入默认检测页。
- 该调整依赖实例固定 `user-data-dir`，因此后续重启改为优先恢复上次关闭前的 Chromium 会话状态。

## 2026-03-31 提交与同步
- 本轮整合改动已整理为提交 `e8c70b0`，提交信息为 `整合代理链路与桌面界面体验优化`。
- 该提交已推送到 `origin/master`。
- 推送后发现记忆库未完整同步，因此补做本次归档与状态重写。
